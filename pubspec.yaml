name: adventures_in
description: Assisting learning and collaboration.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  built_value:
  built_collection:
  redux:
  flutter_redux:

  # remove before app store / play store submission 
  redux_remote_devtools:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  built_value_generator:
  pedantic:
  test:
  mockito:

flutter:
  uses-material-design: true